[package]
name = "notein"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.5"      # Framework UI kita
rfd = "0.14"       # Untuk dialog "Open/Save File" bawaan OS

[build-dependencies]
slint-build = "1.5" # Dibutuhkan jika nanti ingin memisah file .slint